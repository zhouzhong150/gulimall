spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://47.100.60.194:3307/gulimall-sms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: gulimall-coupon

#classpath加*表示不止扫描自己的类路径，引入的其他依赖的类路径也扫描
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto


server:
  port: 7000